# Only needed if using Docker Compose instead of the default embedded database.
# Copy this file to .env and uncomment the line below.

# DATABASE_URL=postgresql://challenge:challenge@localhost:5432/model_registry
